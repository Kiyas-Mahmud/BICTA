node_modules
.nuxt
.output
.data
dist
.env
# ...and any local copy or backup of it. .env.example stays tracked.
.env.*
!.env.example
# Local Worker secrets + miniflare state (local D1 / R2 emulation)
.dev.vars
.wrangler
public/uploads/*
!public/uploads/.gitkeep
*.log

# Internal docs and agent memory: kept on disk, not published to the repo.
*.md
!README.md
